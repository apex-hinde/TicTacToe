const std = @import("std");
const main = @import("main.zig");

//evaluates the current board state from -1 to 1, 1 is player 1 has a high chance of success and visa verse

pub fn eval(board) f32{
    var eval:f32 = 0;
    main.


}
